// File: js-chess-engine.d.ts
// Questo file di dichiarazione serve per tipizzare il modulo "js-chess-engine".
// Poiché questo modulo non fornisce un file di definizione TypeScript,
// definiamo il modulo con un contenuto di tipo "any" per evitare errori di tipizzazione.

declare module 'js-chess-engine' {
  // "content" rappresenta l'intero contenuto del modulo, tipizzato come any.
  const content: any;
  // Esportazione di default del contenuto, in modo da poter importare il modulo con:
  // import jsChessEngine from 'js-chess-engine';
  export default content;
}
